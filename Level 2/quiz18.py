#Function reverses characters in a string.
#returns new string in reversed order.

def reverse_name(backward_name):
    forward_name = ""
    #backward name:
    #length = len(backward_name)-1
    #length = range(0,len(backward_name),-1)
    #length = range(len(backward_name)-1,-1,-1)
    print(length)
    while length >= 0:
        #forward_name += backward_name[index]
        #forward_name += backward_name[length]
        #forward_name += backward_name[length + 1]
        #forward_name += backward_name[len(backward_name) - len(forward_name)]                  
        length = length - 1
    return forward_name

print(reverse_name("nohtyp"))